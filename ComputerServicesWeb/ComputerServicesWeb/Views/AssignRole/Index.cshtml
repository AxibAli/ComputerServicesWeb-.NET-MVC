
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    }

<h2>Assign Role</h2>

@using (Html.BeginForm("Assign", "AssignRole", FormMethod.Post))
{
    <div class="form-group">
        <label for="RolesList">Role</label>
        @Html.DropDownList("RolesList", (IEnumerable<SelectListItem>)ViewBag.RolesList,new {@class="form-control" })
    </div>

    <div class="form-group">
        <label for="UsersList">User</label>
        @Html.DropDownList("UsersList", (IEnumerable<SelectListItem>)ViewBag.UsersList,new {@class="form-control" })
    </div>

    <br/>

    <button type="submit" class="btn btn-primary">Assign</button>

}